mod constant;
mod scope;
mod static_val;
mod variable;

fn main() {
    // variable::declar_variables();
    // variable::declar_mutable_variables();
    // variable::shadowing();

    // constant::use_constant();

    // scope::block_and_scope();

    // static_val::calc_amount(100);
    static_val::calc_total(100);
}
